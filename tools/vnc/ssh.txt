ssh-keygen -t rsa  -b 4096 -f %USERPROFILE%/.ssh/debian_rsa

scp C:/Users/vsheoran.NETAPP/.ssh/debian_rsa.pub vasu@10.195.64.36:~/key.pub
ssh vasu@10.195.64.36


______ Remote ___________

mkdir -p .ssh
cat ~/key.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

______ Check if you can log in without password ______

# Make sure we are able to log in via identity file to server
ssh -i C:/Users/vsheoran.NETAPP/.ssh/debian_rsa vasu@10.195.64.36

#Dont forget to put absolute path of .ssh/config in vscode settings
#You selected "linux" as the remote platform - this will be stored in the setting "remote.SSH.remotePlatform" and can be changed there if needed.
# https://github.com/microsoft/vscode-remote-release/issues/1721#issuecomment-597783966
## "remote.SSH.useLocalServer": false